server.port=8080
spring.application.name=bff-service

# URLs de las funciones serverless (Azure Functions)
# Local: func start corre en 7071 / 7072
# Docker: los servicios exponen puerto 80
usuarios.service.url=${USUARIOS_URL:http://localhost:7071}
prestamos.service.url=${PRESTAMOS_URL:http://localhost:7072}
usuarios.graphql.url=${USUARIOS_GRAPHQL_URL:http://localhost:7073}
prestamos.graphql.url=${PRESTAMOS_GRAPHQL_URL:http://localhost:7074}
